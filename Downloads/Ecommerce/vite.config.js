import { defineConfig } from 'vite';

export default defineConfig({
  // Establece la ruta base para GitHub Pages (nombre del repositorio)
  base: '/ecommerce/', 
  // Le dice a Vite que el directorio raíz del proyecto es donde está este archivo
  root: './', 
});